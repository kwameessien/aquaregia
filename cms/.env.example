# Copy to `.env.local` in this folder (gitignored). Values are not secret, but
# keeping them out of the repo avoids coupling every clone to one project.
# https://www.sanity.io/manage
#
SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=production
