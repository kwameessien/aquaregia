# Next.js — copy to `.env.local` (not committed).
# Use the same project ID and dataset as in `sanity/.env`.

NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=production

# Optional: server-only token for draft/private content (never use NEXT_PUBLIC_*)
# SANITY_API_READ_TOKEN=
